# Xub
A single-user general purpose blogging platform. Markdown based with minimal styling.

## Goals
- Leaning on existing crates where possible
- Avoiding JS as much as possible (currently none)
- Server side rendering